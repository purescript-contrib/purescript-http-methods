{
  "private": true,
  "scripts": {
    "postinstall": "pulp dep install",
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "pulp build"
  },
  "devDependencies": {
    "pulp": "^8.0.0",
    "purescript": "^0.7.6",
    "rimraf": "^2.5.0"
  }
}
